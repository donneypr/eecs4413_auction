apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auction-web 
  namespace: default
spec:
  ingressClassName: traefik
  rules:
  - host: donney.ddns.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: hello
            port:
              number: 80
